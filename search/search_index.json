{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Welcome to the Jolly Family website!","text":"<p>**Under construction!**</p> <p>Welcome to my website! It's pretty empty for now, but it'll grow later.</p> <p>Built with MKDocs.</p>"},{"location":"pages/vms/","title":"Virtual Machines","text":"<p>Written by Keven Jolly</p>"},{"location":"pages/vms/#introduction","title":"Introduction","text":"<p>Welcome to my guide on creating and using virtual machines! If these steps are followed through, you should have a working Debian desktop that can be accessed within your own computer.</p> <p>There are a few tidbits to keep in mind while following this guide:</p> <ul> <li>All steps are performed on a fresh installation of Debian 12.2.</li> <li>I will be using KVM to run my VM.</li> </ul>"},{"location":"pages/vms/#1-prep","title":"1 - Prep","text":"<p>Before we begin creating a virtual machine, there are some steps we need to perform:</p> <ul> <li>Install Virtual Manager</li> <li>Download the disk image of our OS</li> </ul> <p>On any Linux system, you can use your package manager of choice to install Virtual Manager. On Debian and its derivatives, this is accomplished with the <code>apt</code> package manager:</p> <pre><code>sudo apt install virt-manager\n</code></pre> <p>While we could manage our virtual machine exclusively from the command line, using a GUI interface such as Virtual Manager will save us a lot of time.</p> <p>Now, let's download the disk image.  For Debian, the download is directly on the official website, debian.org.</p>"},{"location":"pages/vms/#2-setup","title":"2 - Setup","text":""},{"location":"pages/vms/#21-creating-the-machine","title":"2.1 - Creating the Machine","text":"<p>Let's start Virtual Manager (enter <code>virt-manager</code> in the command line) and see what we can do:</p> <p></p> <p>To begin creating our new virtual machine, click on either <code>File &gt; New Virtual Machine</code>, or click the icon at the top left.</p> <p></p> <p>We are met with the New VM dialogue, and asked how to install the Operating System.</p> <p>Since we have already downloaded the disk image, we will select the option labeled <code>Local install media (ISO image or CDROM)</code>. Continue by pressing the button labeled <code>Forward</code></p> <p></p> <p>Next, we are asked to select the installation media. CLicking <code>Browse...</code> followed by <code>Browse Local</code>, we can find the ISO in our downloads folder using a traditional File Manager dialogue.</p> <p></p> <p>Once the Installation media is selected, you may continue forward.</p> <p>However, it is possible that Virtual Manager does not detect the operating system properly. In that case, uncheck the box at the bottom and search for your chosen operating system, or the closest version to it.</p> <p></p> <p>Now, we need to specify the resources to be allocated to the Virtual Machine. You could allow it access to your full system resources, but you are likely to run into performance issues, especially if you install multiple VM's</p> <p>For a typical computer, I would choose about one quarter or one third of the available resources.</p> <p></p> <p>Our next step is to create the storage device for this Virtual Machine. The default size of 20.0 GiB will suffice, although you may want to configure this if your host machine has a low amount of space available.</p> <p></p> <p>Finally, we are asked to set the name of the Virtual Machine and to review details about the installation. This name can be anything you want, but for this guide I have chosen to name it after the Operating System.</p> <p></p> <p>Now click finish, and wait for the virtual machine to boot for the first time.</p>"},{"location":"pages/vms/#22-installing-debian","title":"2.2 - Installing Debian","text":"<p>Once the virtual machine has booted, you will be greeted with a new window.</p> <p></p> <p>We'll explore this window more in depth later on. For now, we need to finish the installation process.</p> <p>If you have chosen to use Debian 12 for your Operating System, then the next set of steps should apply to you. If you chose something else, your steps may vary.</p> <p>To start, we'll select the <code>Graphical Install</code> option</p> <p>Note: When interacting with the Virtual Machine at this point, your mouse and keyboard may become locked in the window. Pressing Ctrl and Alt at the same time will release control back to the Host system.</p> <p>We will now be asked to complete a number of steps. Keep in mind that many of them can be changed later.</p> <ol> <li> <p>Language</p> <ul> <li> <p>The OS's default literary language</p> </li> <li> <p>My Entry: English</p> </li> </ul> </li> <li> <p>Location</p> <ul> <li>Approximate location of your machine</li> </ul> </li> <li> <p>Keymap</p> <ul> <li>The layout for your keyboard</li> </ul> </li> <li> <p>Hostname</p> <ul> <li>The name your system uses for networking to other machines.       If you are making multiple VM's, this should ideally be unique to each one.       But if you are only making one, call it whatever you like.</li> </ul> </li> <li> <p>Domain Name</p> <ul> <li>The domain name that will lead back to this Virtual Machine.   If you don't plan to using a domain name or to network your VM publicly,   you can skip this step.</li> </ul> </li> <li> <p>Configure Users</p> <p>In this step, we will be configuring both the Root User and any additional users.</p> <ul> <li> <p>Root Password</p> <ul> <li>The password for the root user    Make sure this is something strong, especially if you plan to use your VM as a public server!</li> </ul> </li> <li> <p>User Full Name</p> <ul> <li>Your full First and Last Name, or something else entirely.   This will be used for a non-root user.</li> <li>This may be different from the account username.</li> </ul> </li> <li> <p>Username</p> <ul> <li>The actual username that your user account will use.   Any commands that modify users will use whatever you enter here</li> </ul> </li> <li> <p>User Password</p> <ul> <li>The password for the default user   If you plan on making this VM public, and this user a sudoer, make this a strong password. Otherwise, I'd suggest something easy to remember.</li> </ul> </li> </ul> </li> <li> <p>Clock</p> <ul> <li>Your preference of timezone for the system time.</li> </ul> </li> <li> <p>Disk Partitions</p> <p>We will configure everything about how our Virtual Machine installs onto the disk in this step.</p> <ul> <li> <p>Partitioning Method</p> <ul> <li> <p>Normally, if your disk already had partitions, there would be multiple options available for configuring the installation to work around the existing partitions. However, since we made a new virtual disk for this VM, we don't need those options.</p> </li> <li> <p>For Virtual Machines, I suggest selecting the option: <code>Guided - use entire disk and set up LVM</code></p> </li> </ul> </li> <li> <p>Partition Disks</p> <ul> <li>There's only one option available, so just hit continue.</li> </ul> </li> <li> <p>Partitioning Scheme</p> <ul> <li>Selects how the System Files should be partitioned.   If you're not sure, just use <code>All files in one partition</code></li> </ul> </li> <li> <p>Confirm</p> <ul> <li>You'll be asked to confirm the changes. Select yes and continue.</li> </ul> </li> <li> <p>Volume Group Portion</p> <ul> <li>If you chose to use LVM, you'll be asked how much of the volume to use by default, since it is possible to expand it later. For now, there's no reason to decrease this size, so we can leave this option alone and continue.</li> </ul> </li> <li> <p>Confirm (again)</p> <ul> <li>Again, select yes and continue</li> </ul> </li> </ul> </li> <li> <p>Scan Extra Media</p> <ul> <li>You'll be asked if you want to scan more media. We only needed our Installation ISO, so we can continue without scanning anything more.</li> </ul> </li> <li> <p>Configuring the Package Manager</p> <p>Now we need to configure the Package Manager</p> <ul> <li> <p>Package Manager Mirror</p> <ul> <li>Where the Package Manager will try to download packages from. The default option should be best.</li> </ul> </li> <li> <p>Debian Archive Mirror</p> <ul> <li>Again, the default option should work best</li> </ul> </li> <li> <p>HTTP Proxy</p> <ul> <li>If you have a proxy, you can configure it here. If you don't, just press continue.</li> </ul> </li> <li> <p>Popularity Contest</p> <ul> <li>Whether statistics about the packages you install or use should be collected. This defaults to no, so it's fine to just hit continue.</li> </ul> </li> </ul> </li> <li> <p>Select and Install Software</p> <p>What should be installed by default onto the Virtual Machine. You can choose from a number of Desktop environments, and you can choose to install an SSH and/or a Web server.</p> <ul> <li> <p>8 Desktop Environments are listed</p> <ul> <li> <p>GNOME is one of the most popular DE's, and is the default selection   May be difficult to get used to for Windows users</p> </li> <li> <p>KDE Plasma is highly customizable and stands as a very powerful DE   Great for a standalone system, but a little too heavy for a Virtual Machine</p> </li> <li> <p>LXDE is extremely lightweight, but has similarly light features   Good if you want a GUI with the absolute minimum resource usage</p> </li> <li> <p>XFCE is very well balanced in terms of weight and usability   Great choice for a single Virtual Machine.</p> </li> <li> <p>For now, we will be skipping a DE, as we can simply use the Command Line for setup. Later, we will install XFCE.</p> </li> </ul> </li> <li> <p>Enabling Web server will install an Apache HTTP server to be run by the Virtual Machine. Select this if you plan to host a website on your VM.</p> </li> <li> <p>Enabling SSH server will allow connecting to the VM remotely via the command line of another device. Further steps can be taken to improve the security.</p> </li> <li> <p>There is no reason to disable Standard System Utilities</p> </li> </ul> <p>With that, here is the selection I am using:</p> <p></p> </li> <li> <p>Grub Boot Loader</p> <p>You'll be asked if you want to install GRUB boot loader to the primary drive. Since there's no other OS's on the system, it is safe to do so.</p> <ul> <li>When prompted for a device, select the option listed as <code>/dev/vda</code> or similar.</li> </ul> </li> </ol> <p>Once the Grub Boot loader is configured and installed, the installation is complete. You can now load into your Virtual Machine.</p>"},{"location":"pages/vms/#3-first-boot","title":"3 - First Boot","text":"<p>Note: From here, the order in which you follow the steps may vary. Feel free to jump around and play around with your virtual machine!</p>"},{"location":"pages/vms/#31-virtual-manager-interface","title":"3.1 - Virtual Manager Interface","text":"<p>Before continuing forward, let's talk about the interface. There are plenty of tools to use here, and you may want to take some time to browse around and familiarize yourself with them.</p> <p></p> <p>From left-to-right, the icons are as follows:</p> <ul> <li> <p>First Section</p> <ul> <li> <p>Switch the view to show the display output of the Virtual Machine</p> </li> <li> <p>Swith the view to the Details panel. You'll want to remember this panel for later!</p> </li> </ul> </li> <li> <p>Second Section</p> <ul> <li> <p>Resume a Paused Virtual Machine</p> </li> <li> <p>Pause the Virtual Machine. This will halt all activities until the machine is unpaused.</p> <ul> <li>This can free up the CPU safely if the Virtual Machine is busy</li> </ul> </li> <li> <p>Shutdown Options</p> <ul> <li> <p><code>Reboot</code> and <code>Shut Down</code> will send the appropriate signal to the Machine. This is similar to pressing shutdown or restart on your device</p> </li> <li> <p><code>Force Reboot</code> and <code>`Force Off</code> will immediately shut down the system. This is akin to pulling the power cable, or holding the power button on a device for some time.</p> </li> <li> <p><code>Save</code> will save the current state of the system's RAM to be loaded later. If you need to restart the Host, but the VM is busy, this is a viable option.</p> </li> </ul> </li> </ul> </li> <li> <p>Third Section</p> <ul> <li> <p>Snapshots</p> <ul> <li>Snapshots save the entire state of the System for later restoration. If you need to make potentially destructive changes, visit the Snapshots menu and save the system in case something breaks.</li> </ul> </li> <li> <p>Fullscreen</p> <ul> <li>Fullscreens the VM display.</li> </ul> </li> </ul> </li> </ul>"},{"location":"pages/vms/#32-configuring-the-vm","title":"3.2 - Configuring the VM","text":""},{"location":"pages/vms/#321-modifying-and-adding-users","title":"3.2.1 - Modifying and adding Users","text":"<p>To begin, let's install <code>sudo</code> and allow our default user to use it, so that we do not need to log into root to perform certain actions. We can use <code>apt</code> to install the package, and use <code>usermod</code> to add the user to the <code>sudo</code> group.</p> <p></p> <pre><code>usermod -aG sudo jolly\napt install sudo\n</code></pre> <p>Now, we can log into our default user and verify that we can use <code>sudo</code>.</p> <p></p> <p>We can also add a few users and make them able to use sudo as well.</p> <p></p> <pre><code>sudo adduser -c \"Keven Jolly\" kjolly\nsudo usermod -G sudo kjolly\ngrep \"kjolly\" /etc/passwd\ngrep \"kjolly\" /etc/group\n</code></pre>"},{"location":"pages/vms/#322-ssh-and-serial-port","title":"3.2.2 - SSH and Serial Port","text":"<p>If you'd rather not need to use the Virtual Machine interface to interact with the machine, there a few ways to do so.</p>"},{"location":"pages/vms/#ssh","title":"SSH","text":"<p>If you did not already install SSH during the OS installation, you can do so by installing the <code>openssh-server</code> package.</p> <p>Then, you can use <code>systemctl</code> to activate the SSH system service.</p> <p></p> <pre><code>sudo systemctl enable ssh\n</code></pre> <p>Now, we can (sort-of) remotely connect to this Virtual Machine by using the <code>ssh</code> command:</p> <pre><code>ssh &lt;username&gt;@&lt;ip&gt;\n</code></pre> <ul> <li><code>&lt;username&gt;</code> is the name of the user that you are attempting to log in as.</li> <li><code>&lt;ip&gt;</code> is the ip address or the domain name of the machine you are attempting to connect to.</li> </ul>"},{"location":"pages/vms/#serial-port-console","title":"Serial Port console","text":"<p><code>virsh</code> is a very powerful package for managing Virtual Machines from the command line. If you do not have it installed on the host, I reccomend that you install it.  </p> <p>You can use Virsh to access the console of your VM without using the interface. To do this, you need to enable command line access from the VM's serial port. This can be done with the following command: <code>sudo systemctl enable serial-getty@ttyS0.service</code></p> <p>Once done, you can use Virsh to access your VM from the command line of the Host system.</p> <p></p> <pre><code>sudo virsh console debian12\n</code></pre> <p>Note: Remember to use <code>Ctrl + ]</code> to exit the console at any time.</p> <p>You can also verify that the serial port is working by switching the console to the Serial console in Virtual Manager</p> <p></p>"},{"location":"pages/vms/#323-installing-xfce-desktop","title":"3.2.3 - Installing XFCE Desktop","text":"<p>If you skipped installing a Desktop Environment during the OS installation, you might want to try installing one at this point. Doing so is very easy - we just need to know the package to install.</p> <p>For XFCE on Debian distributions, the package to install is <code>xfce4</code>.</p> <p></p> <p>Note: This installation is quite heavy. You may want to skip ahead to LVM if you need to increase your storage space.</p> <p>After this, you'll want to set the default target to <code>graphical.target</code> using <code>systemctl</code>:</p> <p></p> <p>Now reboot the system, and you should be met with a new login screen.</p> <p></p> <p>If you want to return to a command line only environmnt, you can use <code>systemctl</code> again to set the default target back to <code>multi-user.target</code>.</p> <p></p>"},{"location":"pages/vms/#324-apache2-web-server","title":"3.2.4 - Apache2 Web Server","text":"<p>If you did not choose to install a web server during the OS setup, you can do so by installing the <code>apache2</code> package with <code>apt</code>.</p> <p>Now, if you type your Virtual Machine's IP address into a web browser on the host machine, you'll be met with the Apache2 default page.</p> <p></p> <p>This page's source is located at <code>/var/www/html/index.html</code> within the Virtual Machine, so you may configure it how you please!</p> <p></p> <p>If you want to open access to this page, you'll want to look into a bridge network.</p>"},{"location":"pages/vms/#4-logical-volume-manager","title":"4 - Logical Volume Manager","text":"<p>With Logical Volume Manager, it is possible to append additional storage space to the Virtual Machine without shutting it down. That way, if you need extra storage space, there will be minimal downtime required.</p> <p>Note: We are going to use some potentially dangerous commands. Now would be a good time to make a snapshot!</p> <p>There are a few terms that you should know going forward:</p> Physical Volumes (PV) Volume Groups (VG) Logical Volumes (LV) Physical drives installed into the machine A modifiable group of Physical Volumes A partition of a Volume Group that acts as a single volume <p>A Physical Volume is loaded onto the device, a Volume Group gathers multiple physical volumes together, and a Logical Volume is a single volume carved from one volume group.</p>"},{"location":"pages/vms/#41-adding-physical-volumes","title":"4.1 - Adding \"Physical\" Volumes","text":"<p>To add physical volumes to the virtual machine, we'll want to use Virtual Manager. Navigate the Details panel and select <code>Add Hardware</code></p> <p></p> <p>In the new dialogue, we can select to add a Storage device on the left panel. Then, select the amount of space that you would like to append to the virtual machine.</p> <p></p> <p>For this guide, I will add two storage devices: one with 15 GiB, the other with 25 GiB.</p> <p>In the Details panel, you should now see more VirtIO Disk items, depending on how many devices you added.</p> <p>The machine now has physical storage added, but it has not been configured to recognize it.</p> <p>We'll want to use <code>fdisk</code> to fix this.</p> <p>Start by entering <code>fdisk -l</code> to view all attached volumes. You should spot the volume(s) that you added to the machine. Take note of the device's path.</p> <p></p> <pre><code>sudo fdisk -l\n</code></pre> <p>Now, enter <code>fdisk &lt;path&gt;</code> to start configuring your device.</p> <p>You will find yourself in a console specific to fdisk.</p> <p></p> <pre><code>sudo fdisk /dev/vdb\n</code></pre> <p>From here, there several commands at your disposal. Those of interest are:</p> <ul> <li><code>p</code> - Lists information about the volume</li> <li><code>n</code> - Create a partition on the volume</li> <li><code>t</code> - Sets the Disk Type</li> <li><code>w</code> - Writes the changes to Disk</li> <li><code>q</code> - Exits fdisk</li> </ul> <p>Messing with fdisk too much may cause issues, so follow these next steps closely:</p> <ol> <li>Create a new partition (<code>n</code>). For best case, use all of the default values.</li> <li>Set the disk type to Linux LVM (<code>t</code>). The code we want to enter for this is 8E (Use <code>l</code> to list all codes available).    </li> <li>Write the changes to disk (<code>w</code>) and exit (<code>q</code>).</li> <li>Repeat for each disk that you added.</li> </ol> <p>Once those steps have been performed, use <code>fdisk -l</code> again to view the new partitions.</p> <p></p> <p>We have partitioned disks, but using <code>pvs</code> still shows no physical volumes.</p> <p>We can create physical volumes from the partitioned disks by running <code>pvcreate</code>:</p> <p></p> <pre><code>pvcreate /dev/vdb1 /dev/vdb2\n</code></pre>"},{"location":"pages/vms/#42-volume-groups-and-logical-volumes","title":"4.2 - Volume Groups and Logical Volumes","text":"<p>The physical volumes have now been added to our Virtual Machine, but we can't access it just yet. To do so, we'll need to add the volumes to a Volume Group, and then create some Logical Volumes out of that group.</p> <p>We'll define a new Volume Group with <code>vgcreate</code>.</p> <p></p> <pre><code>sudo vgcreate data /dev/vdb1 /dev/vdb2\n</code></pre> <p>We can see that our Physical Volumes are now contained within a Volume Group. Now, we can add one or more Logical Volumes from this one group, using <code>lvcreate</code>.</p> <p></p> <pre><code>sudo lvcreate -n data01 -L 20.0g data\nsudo lvcreate -n data02 -L 19.9g data\n</code></pre>"},{"location":"pages/vms/#43-mounting-the-volumes","title":"4.3 - Mounting the Volumes","text":"<p>We now have two Logical Volumes carved from a Volume Group that holds two Physical Volumes. The space is now recognized by the system, but there are a few more steps we need to complete before we can use the additional storage space.</p> <p>We need to find the path to our Logical Volumes to use them. We can use this by passing <code>lvdisplay</code> into <code>grep</code>.</p> <p></p> <pre><code>sudo lvdisplay | grep \"Path\"\n</code></pre> <p>The first step to create a file system for each volume. We can use <code>mkfs.ext4</code> to make a filesystem using the <code>ext4</code> format.</p> <p></p> <pre><code>sudo mkfs.ext4 /dev/data/data01\nsudo mkfs.ext4 /dev/data/data02\n</code></pre> <p>Now that the volumes have file systems associated with them, we can mount them to the system with <code>mount</code>.</p> <p></p> <pre><code>sudo mkdir /mnt/data01\nsudo mkdir /mnt/data02\nsudo mount /dev/data/data01 /mnt/data02\nsudo mount /dev/data/data02 /mnt/data02\n</code></pre> <p>We can check that the Filesystems are mounted by creating a file within the mounted directories.</p> <p></p> <pre><code>sudo touch testfile\nls -l\n</code></pre> <p>The volume is mounted and usable, so it works! </p> <p>That is, until the next reboot...</p> <p></p> <pre><code>cd /mnt/data01\nls -l\n</code></pre>"},{"location":"pages/vms/#44-editing-fstab","title":"4.4 - Editing fstab","text":"<p>Our testfile disappeared since, once we rebooted, the logical volume was not remounted.</p> <p>To fix this, we need to add some entries to <code>/etc/fstab</code>.</p> <p>Note: <code>fstab</code> is a dangerous file to edit. Make a snapshot before diving too deep!</p> <p></p> <p>We'll need to add one line to this file for each logical volume we need to mount.</p> <p>These lines hold the path to the filesystem, the directory to mount the volume, the filesystem type along with a few more options.</p> <p></p> <pre><code>/dev/data/data01 /mnt/data01 ext4 defaults 0 0\n/dev/data/data02 /mnt/data02 ext4 defaults 0 0\n</code></pre> <p>Now we need to save the file. On our next reboot, we should have access to our filesystem once again.</p> <p></p> <pre><code>ls -l /mnt/data01\n</code></pre> <p>Our Logical volumes are now mounted on startup, meaning we successfully added more storage to the virtual machine!</p> <p>Alternatively, we can add another layer of reinforcement by entering the device's UUID into fstab instead of the label.</p> <p>We can see the UUID of our Logical Volumes using <code>blkid</code>.</p> <p></p> <p>Then, we enter them in place of the respective label into <code>/etc/fstab</code></p> <p></p> <p>Backup, save, and reboot. Our testfile should be found in <code>/mnt/data01</code> once again.</p> <p>To recap, the steps that we followd were:</p> <ul> <li>Add new Hard Disks through Virtual Manager</li> <li>Partition the disks using <code>fdisk</code></li> <li>Create Physical Volumes using <code>pvcreate</code></li> <li>Create a Volume Groups with <code>vgcreate</code></li> <li>Carve some Logical Volumes with <code>lvcreate</code></li> <li>Mount at runtime using <code>mount</code></li> <li>Enable mounting at boot by editing <code>/etc/fstab</code></li> </ul> <p>Keep in mind that all of these can be repeated without rebooting. Using LVM to add storage space to our Virtual Machine has a lot of steps, but it allows us to do so without causing any downtime (unless we need to restore a backup).</p>"}]}